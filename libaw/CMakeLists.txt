CMAKE_MINIMUM_REQUIRED( VERSION 2.8 )

PROJECT( libaw )

FILE(GLOB LIBAW_SOURCES *.c)
FILE(GLOB LIBAW_HEADERS *.h)

SOURCE_GROUP("Source" FILES ${LIBAW_SOURCES})
SOURCE_GROUP("Headers" FILES ${LIBAW_HEADERS})

ADD_LIBRARY(aw STATIC ${LIBAW_SOURCES} ${LIBAW_HEADERS})
