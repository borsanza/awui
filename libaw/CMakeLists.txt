CMAKE_MINIMUM_REQUIRED( VERSION 2.8 )

PROJECT( libaw )

FILE(GLOB *.c)
FILE(GLOB AWUI_HEADERS awui/*.h)

SOURCE_GROUP("Source" FILES ${AWUI_SOURCES})
SOURCE_GROUP("Headers" FILES ${AWUI_HEADERS})

ADD_LIBRARY(libaw STATIC ${AWUI_SOURCES} ${AWUI_HEADERS})