CMAKE_MINIMUM_REQUIRED( VERSION 2.8 )

PROJECT( libawui )

INCLUDE_DIRECTORIES( ../libaw/ )

FILE(GLOB AWUI_SOURCES awui/*.cpp)
FILE(GLOB AWUI_HEADERS awui/*.h)

SOURCE_GROUP("Source" FILES ${AWUI_SOURCES})
SOURCE_GROUP("Headers" FILES ${AWUI_HEADERS})

ADD_LIBRARY(libawui STATIC ${AWUI_SOURCES} ${AWUI_HEADERS})